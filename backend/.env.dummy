PORT=3000 # Port for the server to run on (ex: 3000)
TOKEN_SECRET=cats123 # Use a strong secret in production (ex: cats123)
DB_PATH=./database.db # Path to the database file (ex: ./database.db)
DB_DIALECT=sqlite # Supported: sqlite, mysql, postgres (sqlite is recommended for development)